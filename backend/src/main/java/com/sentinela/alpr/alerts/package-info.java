/**
 * alerts module — matching against the watchlist (via event), alert
 * generation/querying and real-time push (WebSocket/STOMP).
 * Boundary: exposes a public API; entities/repositories are package-private.
 */
package com.sentinela.alpr.alerts;
