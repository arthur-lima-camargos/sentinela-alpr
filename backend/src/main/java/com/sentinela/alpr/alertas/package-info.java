/**
 * Módulo alertas — matching com a watchlist (via evento), geração/consulta de
 * alertas e push em tempo real (WebSocket/STOMP).
 * Fronteira: expõe API pública; entidades/repositórios são package-private.
 */
package com.sentinela.alpr.alertas;
